package ke.co.myfuture.Myfuture.ApproveQuestion;

import org.springframework.data.jpa.repository.JpaRepository;

public interface ApproveQuestionRepository extends JpaRepository<ApproveQuestion, Long> {
}
